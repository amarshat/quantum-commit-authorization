# zkSync Era native-AA gas measurements

Generated by bench/report.py from bench/measure_zksync.sh. Environment:

```
forge        forge Version: 1.3.5-foundry-zksync-v0.1.9
anvil-zksync anvil-zksync 0.6.11
zksolc       zksolc = "1.5.15"
qca          v0.1.0-20-gd0b99e6-dirty
node         v23.9.0
chainId      260
```

**These numbers are zkSync gas, which is not the same unit as L1 gas.** zkSync charges an ergs-derived amount that folds in the pubdata cost of the transaction's state diff, so a reveal here is not comparable, digit for digit, to the L1 receipt-gas tables in `../contracts/bench`. The point of this harness is not a cheaper number. It is the existence proof: a type-0x71 transaction from `QCAAccountZkSync` validates and executes end to end on a real node, with no ECDSA anywhere on the authorization path and no ERC-4337 bundler relocating the envelope. The forge `--zksync` unit tests cannot show this because their VM does not run the bootloader / NonceHolder validation path; this does.

## Receipt gas per transaction

| depth | commit | reveal (authorization only) | reveal (1 ETH action) |
|---|---|---|---|
| 8 | 130,479 | 152,051 | 168,711 |
| 16 | 130,479 | 158,715 | 175,375 |
| 20 | 130,479 | 162,143 | 178,803 |

## Versus the ECDSA baseline

A zkSync EOA is the `DefaultAccount` system contract, which verifies a secp256k1 signature in its own validation. A plain signed transfer from an EOA is therefore the like-for-like cost of an ECDSA account authorizing the same action, and the gap to our reveal is what the zero-ECDSA machinery (membership proof, nullifier SSTORE, aging, and the account's own validation) adds on top.

| action | ECDSA DefaultAccount | QCA reveal (depth 16) | reveal / ECDSA | commit + reveal / ECDSA |
|---|---|---|---|---|
| authorization only (0-value) | 118,704 | 158,715 | 1.34x | 2.44x |
| 1 ETH transfer | 126,763 | 175,375 | 1.38x | 2.41x |

Two numbers, and the paper must quote both. The single-transaction reveal is about 1.3x an ECDSA transfer. But authorizing one action with commit-reveal is fundamentally *two* transactions, a commit (130,479 gas) and a reveal, so the true per-authorization cost against ECDSA's one transaction is about 2.4x. Leading with the reveal-only ratio and burying the commit would overstate the result.

Both ratios are also a *floor*, not a mainnet figure. anvil-zksync prices pubdata at a fixed low `gasPerPubdata` (168 in this run), and the reveal publishes an extra nullifier state diff that a plain transfer does not. On mainnet `gasPerPubdata` floats with the L1 basefee and is routinely far higher, which charges the reveal's extra state diff more and the baseline's less, so the multiple rises above these values as pubdata gets expensive. The honest read: zero-ECDSA authorization is the same order of magnitude as a signed transfer, at a bounded small multiple, on a live native-AA platform, and the exact multiple is a function of L1 pubdata price.

Commit is depth-independent (it stores one mapping entry). The reveal carries the Merkle proof, so it grows about 841 gas per tree level (one keccak plus the proof word), a small linear term on a large fixed base. The 1 ETH action row adds a value-bearing external call on top of the authorization-only reveal.

## What ran

Per depth and flow: deploy the account via `createAccount` (so it is registered as an AA account), fund it, post a commit, advance chain time past `minCommitAge`, then send the reveal as a type-0x71 transaction whose `customSignature` carries `(leafIndex, secret, proof, maxFeeCap, maxGasCeil, maxPubdataCeil, callGasLimit)`, the full native-AA envelope binding. Every reveal returned status 1. Transaction hashes are in `qca-zksync-receipts.json`; the run is deterministic (fixed secrets), so re-running reproduces the same hashes and gas. Note that the exact reproduction is a property of anvil-zksync's fixed fee model; on Era Sepolia / mainnet the gas moves with the floating pubdata price, so the digits here are emulator constants, not chain constants.
